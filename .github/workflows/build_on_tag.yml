name: Docker Image Build

on:
  push:
    tags:
      - '**'
      - '!*docker*'

  workflow_dispatch:

jobs:
  build-image:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        branch: [master, other]
        include:
          - branch: master
            tags: "type=raw,priority=900,value=latest\ntype=raw,priority=700,value=4.2.2\ntype=raw,priority=500,value=4.0\ntype=raw,priority=300,value=4\ntype=raw,priority=100,value=stable\n"

    steps:
     - name: Checkout Code
       uses: actions/checkout@v3

     - name: Set Metadata
       id: meta
       uses: docker/metadata-action@v4
       with:
         images: rockstorm/git-server
         tags: ${{ matrix.tags }}|
          
     - name: Echo Results
       run: |
         echo "${{ matrix.branch }}"
         echo "${{ steps.meta.outputs.tags }}"
